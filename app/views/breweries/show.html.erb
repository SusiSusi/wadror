<p id="notice"><%= notice %></p>

<h2><%= @brewery.name %></h2>
<i>Established at <%= @brewery.year %></i>

<p>
  Number of beers <%= @brewery.beers.count %>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
      <%= link_to beer.name, beer %> |
  <% end %>
</p>

<% if @brewery.ratings.empty? %>
    Brewery's beers has not yet been rated!
<% else %>
    Has <%= pluralize(@brewery.ratings.count, 'rating') %>, average <%= @brewery.average_rating %>
<% end %>

<br>
<br>
<br>

<%= link_to 'Edit', edit_brewery_path(@brewery) if current_user %>
<br>
<%= link_to 'Destroy brewery', @brewery, method: :delete, data: { confirm: 'Are you sure?' } if current_user %>

